/*
package com.jason.purchase_agent.repository.jpa;

import java.time.LocalDateTime;

public interface ProcessStatusCustomRepository {
    void insertBatch(String batchId, String step, String message, String status, LocalDateTime startedAt);

    void updateStatus(String productCode, String status, String step, String message, LocalDateTime now);

    void updateBatchStatus(String batchId, String status, String step, String message, LocalDateTime now);
}*/
